name: CICD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: CICDjob
    steps:
    - uses: actions/checkout@v3
    - name: cmake
      run: cmake -S . -B ./build/
    - name: make
      run: cd build && make
    - name: test
      run: ./build/tests/number_tests
